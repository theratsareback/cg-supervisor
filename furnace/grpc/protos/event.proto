syntax = "proto3";
option csharp_namespace = "furnace.grpc";

service Events {
    rpc SendEvent (Event) returns (EventResponse);
}

message Event {
    uint32 type = 1;
    uint32 index = 2;
    string payload = 3;
}

message EventResponse {
    uint32 index = 1;
    string payload = 2;
}
